# =============================================================
# CoinAutoTrading Bot - 의존성 패키지
# =============================================================
# 설치: pip install -r requirements.txt
# Python 3.10 이상 권장

# ── Upbit API 클라이언트 ──
pyupbit==0.2.33

# ── 데이터 처리 ──
pandas==2.2.3
numpy==1.26.4

# ── 기술적 분석 지표 (순수 Python, C 의존성 없음) ──
ta==0.11.0

# ── 설정 관리 ──
PyYAML==6.0.2
python-dotenv==1.0.1

# ── WebSocket (실시간 시세 수신) ──
websockets==13.1

# ── 개발/테스트 도구 ──
pytest==8.3.4
pytest-asyncio==0.24.0
